<h1 class="form-control">GOSSIPS</h1>
<div class="list-group-item list-group-item-action flex-column align-items-start active">
  <p><strong>Pseudo du Gossip : </strong><%= Gossip.find(params["id"]).user.first_name %></p>
</div>
<div class="d-flex w-100 justify-content-between">
  <p><strong>Contenue du Gossip : </strong><br> <%= Gossip.find(params["id"]).content %></p>
  <p><strong>Date de publication : </strong><%= Gossip.find(params["id"]).created_at %></p>
</div>
